'use strict'

// Bucle FOR

// Loops, Iteradores, Ciclos

// Estructura de control que se repite varias veces, definido u indefinido


let numero = 100;

for (var i = 0; i <= numero; i++) {
    console.log("Vamos por el numero:" + i);
    debugger;
}